from tkinter import *

window = Tk()
window.title("Finalização Automatizada de Itens - BACKLOG")
texto = Label(window, text="Clique no botão para adicionar a execução do BOT nas funções automáticas do seu sistema")
texto.grid(column=0, row=0, padx=10, pady=10)

window.mainloop()